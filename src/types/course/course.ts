export interface Course {
  id: string;

  title: string;
  slug: string;
  description: string;
  thumbnailURL: string;
  difficulty: string;
  totalLessons: string;

  createdAt: string;
  updatedAt: string;
}
